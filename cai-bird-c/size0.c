#include <stdio.h>
#include <limits.h>
#include <float.h>

int main()
{
    printf("int size: %lu \n", sizeof(int));

    printf("int- size: %lu \n", sizeof(int));

    printf("floag- size: %lu \n", sizeof(float));

    // return 0;
}